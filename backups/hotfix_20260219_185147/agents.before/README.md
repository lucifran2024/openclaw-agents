# agents/ legacy overlays

Canonical agent instructions and playbooks live in `workspace/agents/<id>`.

This `agents/` tree is kept for compatibility overlays (for example `agent/models.json` and `agent/auth-profiles.json` in some agents).

When editing prompts, heartbeat, identity or tools, use `workspace/agents/<id>` as source of truth.